import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gracefulseahorsedash',
  appName: 'graceful-seahorse-dash',
  webDir: 'dist'
};

export default config;
